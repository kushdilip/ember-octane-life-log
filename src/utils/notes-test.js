import notes from 'ember-octane-life-log/utils/notes';
import { module, test } from 'qunit';

module('Unit | Utility | notes', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = notes();
    assert.ok(result);
  });
});
